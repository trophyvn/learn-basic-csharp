TaskManager taskManager = new TaskManager();

// Tập trung vào việc sử dụng các tính năng cốt lõi mà người dùng thực sự cần
taskManager.AddTask(new Task(/* ... */));
taskManager.CompleteTask(taskId: 1);
